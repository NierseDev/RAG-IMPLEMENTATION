# Agentic RAG System - Environment Configuration
# Copy this file to .env and fill in your actual values

# ============================================================================
# Supabase Configuration (REQUIRED)
# ============================================================================
# Get these from: https://supabase.com/dashboard/project/_/settings/api
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# ============================================================================
# Ollama Configuration
# ============================================================================
# Ollama server URL (default: local)
OLLAMA_BASE_URL=http://localhost:11434

# LLM model for agent reasoning (qwen3.5:4b recommended for balance)
# Other options: qwen2.5:7b, llama3.2:3b, mistral:7b
OLLAMA_LLM_MODEL=qwen3.5:4b

# Embedding model (mxbai-embed-large recommended, 1024 dimensions)
# Other options: nomic-embed-text, all-minilm
OLLAMA_EMBED_MODEL=mxbai-embed-large

# Ollama API timeout in seconds for long-running operations
OLLAMA_TIMEOUT=300

# ============================================================================
# Context Length Configuration (NEW - Prevents context length errors)
# ============================================================================
# LLM model context window size in tokens
# Adjust based on your model (check with: ollama show <model> --modelfile)
# Common values: 4096, 8192, 16384, 32768
LLM_CONTEXT_WINDOW=8192

# Embedding model context window size in tokens
# mxbai-embed-large: 512, text-embedding-3-*: 8192
EMBEDDING_CONTEXT_WINDOW=512

# Maximum tokens for LLM output/response
MAX_OUTPUT_TOKENS=1024

# Reserved tokens for system prompt and overhead
CONTEXT_RESERVE_TOKENS=1500

# ============================================================================
# RAG Configuration
# ============================================================================
# Chunk size in tokens (MUST be less than EMBEDDING_CONTEXT_WINDOW - 50)
# Reduced from 800 to 400 to fit embedding model limits
CHUNK_SIZE=400

# Overlap between chunks in tokens (proportional to chunk size)
CHUNK_OVERLAP=50

# Number of similar chunks to retrieve per query
TOP_K_RESULTS=5

# Maximum chunks to include in LLM context (prevents overflow)
# Formula: CHUNK_SIZE * MAX_CONTEXT_CHUNKS should be < LLM_CONTEXT_WINDOW * 0.6
MAX_CONTEXT_CHUNKS=8

# Minimum similarity score (0.0-1.0, 0.0 = no filter)
MIN_SIMILARITY=0.0

# ============================================================================
# Agent Configuration
# ============================================================================
# Maximum reasoning loop iterations (1-5, 3 recommended)
MAX_AGENT_ITERATIONS=3

# Minimum confidence to provide answer (0.0-1.0, 0.7 recommended)
MIN_CONFIDENCE_THRESHOLD=0.7

# Enable hallucination verification (true/false)
ENABLE_VERIFICATION=true

# Enable streaming responses (experimental)
ENABLE_STREAMING=false

# ============================================================================
# Document Processing
# ============================================================================
# Maximum file size for uploads in MB
MAX_FILE_SIZE_MB=50

# ============================================================================
# API Configuration
# ============================================================================
API_TITLE=Agentic RAG API
API_VERSION=1.0.0
API_PREFIX=

# CORS allowed origins (comma-separated, * for all)
CORS_ORIGINS=*

# ============================================================================
# Logging & Development
# ============================================================================
# Log level: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO

# Enable debug mode for detailed logs
DEBUG_MODE=false
